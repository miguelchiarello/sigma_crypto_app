import 'package:flutter/material.dart';

class CurrenciesListTile extends StatelessWidget {
  const CurrenciesListTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
